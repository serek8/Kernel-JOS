#include <kernel/swap/swap.h>
#include <stdio.h>


int swap_out(struct page_info *page){
    // cprintf("ptr=%a\n", page->pp_rmap);
    return -1;
}

int swap_in(uint64_t swap_index){


    return -1;
}